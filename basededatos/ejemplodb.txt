Create table usuario
(numero_usuario SERIAL PRIMARY KEY,
nombre_usuario TEXT,
localizacion_usuario TEXT,
email_usuario VARCHAR,
rol TEXT,
contraseña_usuario TEXT
);

Create table residuo
(nombre_residuo TEXT,
material_residuo TEXT,
descripcion_residuo VARCHAR,
foto_residuo FOTO

);

Create table punto_acopio
(nombre_punto TEXT,
ubicacion_punto TEXT,
tipo_residuo TEXT

);
CREATE TABLE contenedor
(color_contenedor TEXT,
nombre_contenedor TEXT,
descripcion_contenedor VARCHAR
);

Create table tip
(nombre_tip
);